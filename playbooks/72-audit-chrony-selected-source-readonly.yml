---
- name: 72 Audit Chrony Selected Source Readonly
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Audit Chrony selected source
      ansible.builtin.shell: |
        chronyc sources -v || true
chronyc tracking || true
      args:
        executable: /bin/bash
      register: result_37535
      changed_when: false
      failed_when: false

