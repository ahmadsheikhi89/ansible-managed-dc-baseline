---
- name: Install Lynis on Rocky Linux from internal repository
  hosts: rocky_managed
  gather_facts: true
  become: true
  serial: 1
  tasks:
    - name: Install Lynis package
      ansible.builtin.dnf:
        name: lynis
        state: present
