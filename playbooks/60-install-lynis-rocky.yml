---
- name: 60 Install Lynis Rocky
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Example Lynis install command for Rocky
      ansible.builtin.shell: |
        echo "Example only: test with --check --diff before enabling package installation."
      args:
        executable: /bin/bash
      register: result_67231
      changed_when: false
      failed_when: false

