---
- name: 19 Classify Os
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Classify OS family
      ansible.builtin.shell: |
        cat /etc/os-release || true
      args:
        executable: /bin/bash
      register: result_48621
      changed_when: false
      failed_when: false

