---
- name: 70 Move Ubuntu Apt Backup Dirs
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Preview Ubuntu APT backup directory cleanup
      ansible.builtin.shell: |
        echo "Example only. Archive and cleanup must be tested with --check --diff."
      args:
        executable: /bin/bash
      register: result_55221
      changed_when: false
      failed_when: false

