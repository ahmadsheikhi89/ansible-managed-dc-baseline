---
- name: 59 Audit Lynis Availability
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Audit Lynis availability
      ansible.builtin.shell: |
        command -v lynis || true
      args:
        executable: /bin/bash
      register: result_87420
      changed_when: false
      failed_when: false

