---
- name: 25 Configure Ubuntu Apt Repos
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Dry-run Ubuntu APT repository policy
      ansible.builtin.shell: |
        echo "Example only. Run with --check --diff before changing production."
      args:
        executable: /bin/bash
      register: result_40469
      changed_when: false
      failed_when: false

