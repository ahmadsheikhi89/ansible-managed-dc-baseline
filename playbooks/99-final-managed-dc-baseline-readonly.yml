---
- name: 99 Final Managed Dc Baseline Readonly
  hosts: baseline_readonly_scope
  gather_facts: true
  become: true
  serial: 1
  tasks:

    - name: Run final local synthetic report note
      ansible.builtin.shell: |
        echo "Use scripts/generate-demo-data.py for demo reports or adapt delegated localhost report tasks for real audits."
      args:
        executable: /bin/bash
      register: result_64618
      changed_when: false
      failed_when: false

